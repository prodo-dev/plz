#!/bin/bash

set -e
set -u

export PYTHONPATH=./src

exec gunicorn \
  --bind="0.0.0.0:${PORT:-8080}" \
  --preload \
  --workers=16 \
  --timeout=120 \
  --pythonpath=${PYTHONPATH} \
  plz.controller.main:app \
  -- $@
